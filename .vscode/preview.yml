# .vscode/preview.yml
autoOpen: false
apps:
  -port: 3000
   run: chmod +x deploy.sh && bash deploy.sh && lt --port 3000 && cd /workspace/theia-app yarn theia start --hostname 0.0.0.0 
   root: /workspace
   name: theia
   description: IDE
   autoOpen: true
   autiPreview: false
