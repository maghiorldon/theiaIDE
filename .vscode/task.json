{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Deploy and Start Theia",
      "type": "shell",
      "command": "chmod +x deploy.sh && bash deploy.sh && lt --port 3000 && cd /workspace/theia-app && yarn theia start --hostname 0.0.0.0",
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
